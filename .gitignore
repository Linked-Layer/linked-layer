node_modules/
dist/
*.tsbuildinfo
# NOTE: .env is COMMITTED for the Forgejo deploy platform (deploy config; no secrets).
# Real secrets go in Forgejo -> Settings -> Actions -> Secrets (see RUNTIME_KEYS).
.env.local
*.log
.DS_Store
coverage/
.pnpm-store/
